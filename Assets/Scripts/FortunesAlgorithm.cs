using System.Collections;
using System.Collections.Generic;
using UnityEngine;

class FortunesAlgorithm {
    public static void Generate(List<Vector2> sites, Vector2 dimensions) {
        float sweepLine = 0.0f;

        sites.Sort(); // sort by x-value

        // move through sorted sites and apply Fortune's Algorithm
        foreach (Vector2 site in sites) { }
    }

    // public static LinkedList<> Run() {
    //     return null;
    // }
}